apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init-config
  namespace: clo835-app
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS employees;
    USE employees;
    
    CREATE TABLE IF NOT EXISTS employees (
      emp_id INT NOT NULL,
      first_name VARCHAR(40) NOT NULL,
      last_name VARCHAR(40) NOT NULL,
      primary_skills VARCHAR(100) NOT NULL,
      location VARCHAR(100) NOT NULL,
      PRIMARY KEY (emp_id)
    );
    
    -- Insert sample data
    INSERT IGNORE INTO employees (emp_id, first_name, last_name, primary_skills, location) VALUES
    (1, 'John', 'Doe', 'Python, Docker, Kubernetes', 'Toronto'),
    (2, 'Jane', 'Smith', 'Java, Spring Boot, AWS', 'Vancouver'),
    (3, 'Bob', 'Johnson', 'JavaScript, React, Node.js', 'Montreal'); 